# masest-027 implementation

## Changed files

- `package.json`: added explicit `qa:ui-critical`; appended it after built-site and commerce validation in `verify`.
- `css/style.css`: disabled meaningful animation/transition timing and smooth scrolling under reduced motion.
- `css/blog.css`: removed blog-card hover transform/shadow under reduced motion.
- `tests/build-architecture.test.mjs`: locked exact suite membership, no wildcard, and `verify` ordering.
- `tools/story-hmis-visual.spec.mjs`: added computed-style coverage for global CTA and blog-card reduced-motion behavior.
- `advisor-plans/README.md`: marked row 027 `DONE`.

## Checks

- RED: `node --test tests/build-architecture.test.mjs` failed because `qa:ui-critical` was absent.
- RED: focused reduced-motion Playwright case failed on CTA `transitionDuration`.
- GREEN: both focused contracts passed.
- `npm run qa:ui-critical`: 42 passed in 30.2s (30.70s wall).
- `npm run qa:ui-critical && npm run qa:ui-critical`: 42 passed in 29.7s, then 42 passed in 29.9s (60.63s wall); no port/lifecycle failure.
- `npm run check`: 214 JavaScript files checked.
- `npm run verify`: 1,528 Node tests passed; build copied 405 files; site verification checked 91 HTML, 7 CSS, and 167 JS files; commerce smoke 12 passed; critical UI 42 passed.

## Implementation note / open risk

No numeric local-gate budget appears in the spec or plan. Smallest reversible assumption: measured 29.7–30.4s single-run critical gate is acceptable. If maintainer budget is lower, extract only named contracts from the two broad buyer-story specs into a focused explicit spec; do not add retries or a wildcard.
