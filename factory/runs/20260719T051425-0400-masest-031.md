# masest-031 implementation run

- Spec hash: `4228e80d58d465986f8a6b8ad83678648ee8bfa2fd4850ff67dc8242b8a1403e`
- Native adapter: `codex`
- Outcome: lazy-group implementation complete; review gate blocked by two pre-existing rich-editor failures outside the spec's allowed source files.
- Status row: left `TODO`; do not mark `DONE` until the required browser command is green.
- Active spec: left in `factory/specs/active/masest-031.md`.

## Implementation

- Added `js/admin/feature-loader.js`: cached construction, normalized `render`/`wire`, one-time wiring, and failed-build eviction.
- Reworked `js/admin.js` so overview/auth/shared shell code stays eager while the specified feature groups use release-coupled dynamic imports.
- Added navigation tokens, pending-load invalidation, and ordered render commits so stale loads and already-running renders cannot finish after the current workspace.
- Added deterministic import-failure UI. Retry reloads the current hash because failed module records remain poisoned for the current document; no cache-busting exception was added.
- Kept the support badge eager from `/api/admin/stats`; review-specific badge refresh remains inside the Reviews group.
- Preserved aliases, history routing, deep-link context, permission reapplication, dirty-edit ownership, and cache-release traversal.

## Focused test changes

- Added loader unit coverage in `tests/admin-split.test.mjs`.
- Added overview-zero-import, stale-load, stale-in-flight-render, and failed-import recovery coverage in `tools/admin-auth-gate.spec.mjs`.
- Updated source-contract tests to require dynamic rather than eager feature imports.
- Updated existing CMS/support browser flows to wait for lazy workspaces and use the current shared image/reference interfaces.

## Verification

- PASS: `node --test --test-timeout=120000 tests/auth-cache-release.test.mjs tests/admin-information-architecture.test.mjs tests/admin-role-aware-ui.test.mjs tests/admin-split.test.mjs` — 20/20.
- FAIL: `npx playwright test tools/admin-auth-gate.spec.mjs tools/admin-content-cms.spec.mjs tools/admin-quote-message-flows.spec.mjs --reporter=line` — 21/23; the six lazy-loader/auth tests pass.
- PASS: `npm run check` — 219 JavaScript files.
- PASS: `npm run verify` — unit/build/site/commerce/UI gates, including site verification, 12 commerce tests, and 42 critical UI tests.

## Open blockers

1. `js/admin/rich-editor.js` `wrapSelection()` calls undeclared `selection.removeAllRanges()`. The size wrapper reaches the editor DOM, then the exception prevents output synchronization. This is outside the spec's source-file scope.
2. Newsletter output contains the product reference card, but `#nlPreview` renders that first card token as text while rendering the later service card. Repair belongs to the existing rich-editor/Markdown preview path, also outside this spec.

No other correctness, architecture, security, or performance findings remain from self-review.
